# 断点修复实施计划书（可执行版）

> 本文是 `断点修复方案.md`（设计级）的**执行落地版**：把每条修复落成「改哪个文件 / 怎么改 / 改完怎么验 / 依赖什么」的可勾选任务。
> 所有行号沿用 `断点修复方案.md·附录 A`（已核）。架构铁律：**一切外部流量仍经 `localTool :18080` / `/api/proxy`，不得绕过。**
>
> 状态图例：`☐ 待做` / `☑ 已做` / `⛔ 卡住（需用户/外部输入）`。

---

## 〇、动手前必须先解决的 2 件事（blocker）

| # | 事项 | 现状 | 谁解决 | 卡点说明 |
|---|---|---|---|---|
| **B1** | 真实 Lovart AK/SK 有效性 | `.env` 已是 `ak_ff40…`/`sk_63a1…` 真实形态（F1 `[已具备]`），但 lovart 侧是否仍有效**未知** | **用户确认** | 无效则 P0 修完生图仍静默失败（启动日志无 `⚠️ 占位符` 警告≠密钥有效） |
| **B2** | `sync/default` 基线 `apiConfigs` 样本 | 路由未实现，且内容需一份真实官方响应 | **需抓取存档** | 编码前先抓一份官方 `GET /api/sync/default` JSON，存 `localTool/data/apiConfigs.baseline.json`；否则高-4 无法落地 |

> 其余前置 F2（端口 9004 已对齐 `[已落地]`）、调试基建（探针/日志 `[已落地]`）已完成，无需再做。
> 若 B1 暂不确认，可**先实施 P0 + 非生成类修复**（L2/L3/I1 等），仅"真生图/对话"需 B1 才验证。

---

## 阶段 0：合并启动器（I1 / F3，低风险，建议最先做体验闭环）

| 任务 | 文件 / 动作 | 验收 | 状态 |
|---|---|---|---|
| 新建 `launch-all.sh`/`.command` | 顺序：① `localTool/start.sh` ② `apimart-gateway/启动网关.command` ③ `open http://127.0.0.1:18080` | 一键启动后两服务在线、画布自动弹出 | ☐ |

---

## 阶段 1：P0 三件套（解锁一切生成/对话，最高优先）

> `localTool/src/routes/system.ts`。改完 `npm run build` → 重启 `node dist/index.js` → 探针回归。

### P0-1 解除 15s 硬超时
- **改**：`system.ts:68`、`system.ts:132` 两处 `setTimeout(() => controller.abort(), 15000)` → 读 `process.env.PROXY_TIMEOUT`（默认 `300000`）。
- **验收**：提交 >15s 视频任务，`[proxy] ... | TIMEOUT` 日志消失（`system.ts:97/:160`）。
- **状态**：☐

### P0-2 转发 SSE 流式
- **改**：`handleProxyFormData`（`system.ts:80,92-93`）与 `handleProxyJson`（`system.ts:144,155-156`），当下游 `Content-Type:text/event-stream` 或请求体 `stream:true` 时：不 `arrayBuffer()` 缓冲，改为 `res.writeHead(status, cleanHeaders)` 后 `Readable.fromWeb(fetchRes.body).pipe(res)`；**绝不设 `Content-Length`**（chunked）。
- **致命坑**：`skipHeaders`（`system.ts:85/:148`）会剔除 `content-encoding`。**推荐方案(a)**：pipe 前本地 `zlib`/`undici` `decompressed()` 解压再传纯文本；或方案(b)保留 `content-encoding` 头随字节透传。推荐 (a)，避免前端解码依赖。
- **验收**：调试面板请求日志里 `chat/completions` 出现持续分块响应（非一次性 200）；压缩响应不乱码。
- **状态**：☐

### P0-3 协议翻译层（与高-1 职责唯一化）
- **决策**：网关（G1/G2/G3）只 alias/透传 Lovart 原生响应，**不拆 `{code,data}`、不做前端形状映射**；所有字段映射集中在这里。
- **改**：`handleProxy`（`system.ts:24-39`）返回处加轻量适配——识别 `body.code`/`body.data` 信封，按端点映射成前端形状（S5 draw 取 `data[0].task_id` 已核）；未知结构原样透传降级。
- **验证明细（已核-dist）**：G2 期望 `status==="succeeded"`+`results[0].url`（`httpClient:561`）；draw 期望 `data[0].task_id`（`main.py:781` 信封 `{code:200,data:[...]}`）。
- **状态**：☐

---

## 阶段 2：高-4 `sync/default` 本地兜底（A2，依赖 B2 样本）

- **改**：`index.ts` 路由表加 `GET /api/sync/default`，返回 `localTool/data/apiConfigs.baseline.json`，把其中 `{VITE_API_BASE_URL}` 占位符替换为 `http://127.0.0.1:18080`。
- **验收**：探针 `sync-default` 由 `warn(404)` 转 `ok`；官方离线时节点库仍有默认节点/模型。
- **依赖**：⛔ 需 B2 样本 JSON 先就位。
- **状态**：☐

---

## 阶段 3：高-1 网关 G1/G2/G3 + 高-2 画布宿主（L2）

### 高-1 网关补视频轮询 + draw（C 域 `main.py`）
- **改**：新增 `GET /v1/videos/{id}`（G1，alias 到 `/v1/tasks/{id}:873`，原样透传 Lovart `tasks` 原生字段）、`GET /v1/video/generations/{id}`（G2，alias 到 kkidc 上游——**先确认 `video`/`sd2Video`/`discountVideo` 各自真实上游，勿直接 alias 到 Lovart 会 404**）、`POST /v1/draw/completions`（G3，复用 `/v1/images/generations:591` 逻辑，接受 `{model,prompt,aspectRatio,urls}`）。
- **架构**：仅 alias + 透传，不拆信封（映射交 P0-3）。
- **风险**：G1/G2 是否真被前端调用待核（视频轮询可能已走 `/v1/tasks/{id}`，别名或冗余，动手前核前端真实调用点）。
- **验收**：探针 `gw-videos-poll`/`gw-images-draw` 返回 200；字段解析由 P0-3 + S5 联调确认。
- **状态**：☐

### 高-2 画布页面宿主 + 自动开浏览器（L2）
- **改**：`index.ts:45-97` 仅处理 `/files/*`，新增根路由 `/`、`/index.html` 托管 `localTool/../dist/index.html`（方式一同源）；启动后 `open('http://127.0.0.1:18080')`（Mac `open`/Win `start`）。
- **验收**：启动后自动弹出画布页面。
- **状态**：☐

---

## 阶段 4：中优先（本地缺口补齐）

| # | 任务 | 文件 / 动作 | 验收 | 状态 |
|---|---|---|---|---|
| 中-1 | L1 `kv/delete` | `index.ts:129-134` 加 `POST /api/kv/delete?key=`，复用 kv 存储删 key（`kv.ts` 无 `handleKvDelete`） | 探针 `kv-delete` 由 warn 转 ok | ☐ |
| 中-2 | L5 `assets/upload`、`upload/app-asset` | `index.ts` 加两路由，返 `{success:true,data:{url}}`；**勿重复**已存在的 `/api/files/upload`(`:137`) | 探针 `upload-app-asset` 转 ok | ☐ |
| 中-3 | S1 相对路径破图 | `files.ts:40-44`(`url`)、`files.ts:234`(`thumbnailUrl`) 改返绝对路径 `http://127.0.0.1:18080/files/...`（方式二 `chrome-extension://` 防 404） | 方式二下资源图/缩略图不破 | ☐ |
| 中-4 | P2 ffmpeg 审计 | `system.ts:18` 已 `ffmpeg:false`；待定：比对原版是否内置 ffmpeg、前端后处理是否依赖 | 拍板是否需内置 ffmpeg 并上报 true | ☐ 待定 |
| 中-5 | I3 预编译 | 确认 `start.sh` 含 `npm run build`，或提交 `localTool/dist/` 开箱即用 | 直接 `node dist/index.js` 可跑 | ☐ |
| 中-6 | G4 `assets/upload` | `main.py` 加 `POST /api/assets/upload`（Bearer+JSON）或 alias `/v1/uploads/images`(`:900`) | 网关该路由 200 | ☐ |
| 中-7 | G5 `ai-app/*` | 仅启用 AI 应用市场时实现 `/v1/gateway/ai-app/*` + `/api/ai-apps`；核心生图/视频不依赖，可暂缓 | 视功能开关 | ☐ 可选 |
| 中-8 | A1/A3/A4 官方兜底 | 把助手 `vip-check`/`chat`、工作流 `workflow-apps`、资产 `app-asset` 重定向到 localTool 兜底或官方（注：已核-dist 显示扩展内直连 `127.0.0.1:18080`+读 kv `active_api_endpoint`，"纯官方无兜底"需重判） | 联调确认三能力在官方不可达时可用 | ☐ |

---

## 阶段 5：低优先 + 联调（不影响核心生图/视频/对话）

| # | 任务 | 文件 / 动作 | 状态 |
|---|---|---|---|
| 低-1 | L3 版本统一 | `platform.ts:13`(`1.3.5`)、`index.ts:24`、`system.ts:9`(`2.0.0-maomao-clone`) 统一为真实版本号 | ☐ |
| 低-2 | S2 缩略图真缩放 | `files.ts:120,230` 集成 `sharp` 做真缩放（否则维持 `copyFileSync` 降级，功能可用） | ☐ 可选 |
| 低-3 | S3/S4 字段名联调 | 联调 `/api/tasks`、`/api/resources`（paginated `{items,...}`）、`/api/kv/get`（裸值）与前端取字段一致；已核-dist：`rowToTask`/`rowToResource` 为字段源头 | ☐ |
| 低-4 | I4 安全整改 | `启动网关.command:77` `0.0.0.0`→`127.0.0.1`（真正暴露面）；`apimart-gateway/.env:9` `OPEN_RELAY=true` 加 token/白名单；`index.ts:106` CORS `*`→`127.0.0.1`。注：网关全文件 CORS 0 命中，是"新增"非"收紧" | ☐ |
| 低-5 | G6 听音 | `main.py:667` 外接第三方 STT 或置灰「听音」入口 | ☐ |
| 低-6 | S5 task_id | 已核-dist 形状一致（`data[0].task_id`），联调固化即可 | ☐ |
| 低-7/8/9 | A5/A6/A7 官方基址 | 离线环境去硬编码公网 fallback（`154.219.102.3012`等）；`0.1mao.cc` 实为官方主基址占位、无害 | ☐ |

---

## 验收方法（复用已落地调试基建，三层验证）

1. **连通层**：重载扩展 → 侧边栏 🔍 → ▶ 探针。P0 后 `gw-*` 由 NET_ERR/404 转 ok；高-4 后 `sync-default` 转 ok。探针共 **16 端点**（11 B 域 + 5 gw-*），无 `gw-videos-poll2`。
2. **解析层**：请求日志 Tab 过滤 `localhost|127.0.0.1|9004`，看 `chat/completions` 是否流式分块（P0-2）；`grep '[proxy]'` 看超时/错误。
3. **功能层**：真生图/生视频/对话成功。**三层全过才算修好。**

---

## 安全操作与回退

- 已 `git init`（基线快照「改动前可跑版本」）。**一次只改一个断点**，`git add -A && git commit -m "fix: xxx"` 形成可回退链。
- 改 `src/*.ts` 前先 `npm run build`（tsc 类型检查，未成功不覆盖 `dist/`，原服务不受影响）；build 过但功能坏 → `git checkout` 还原 `src` 重 build 重启。
- 网关 `main.py` 单文件，改完直接重启 uvicorn；崩了还原 `main.py` 重启。
- 扩展改 `dist/` 后 `chrome://extensions` 点「重新加载」。

---

## 推荐执行顺序（一句话）

`B1/B2 拍板` → `阶段0 启动器` → `阶段1 P0 三件套（命门）` → `阶段2 sync/default（需 B2）` → `阶段3 G1/G2/G3 + L2` → `阶段4 中-1~8` → `阶段5 低-1~9 + 联调`。

> 每阶段结束跑一次探针 + 三层验证再进下一阶段。P0 不修，后面全白搭。
