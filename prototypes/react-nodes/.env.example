# 画布 AI 助手（AgentPanel）LLM 配置 —— 复制为 .env 后按需修改
# 这些变量被 src/components/base/useAgentChat.js 读取。

# ── 演示模式（推荐先开，零配置体验）────────────────────────────
# 设为 1 时，Agent 用本地规则引擎模拟 LLM：说一句「创建一个生图节点」，
# 就能看到画布实时变化，不需要任何 API key。
# 只识别：创建节点(生图/视频/文本)/连接两个节点/删除节点/查看画布/适配视图。
VITE_AGENT_DEMO=1

# ── 方案 A（推荐，默认）：走 localTool 中继 ──────────────────────
# 原型默认会把 /api/agent/canvas-assistant/chat 请求发给 localTool(18080)，
# 由 localTool/src/routes/agentChat.ts 再转发到「支持 function calling」的 LLM。
# 前提：本机 localTool 已在 18080 运行，且其 .env 已配 LLM_CHAT_BASE_URL（见 docs/27 §3/§11）。
# 这样无需在原型配 LLM key，复用 localTool 的通道。
# VITE_LLM_CHAT_BASE_URL=

# ── 方案 B：直接连某个 OpenAI 兼容端点 ──────────────────────────
# 若不想经 localTool，直接指到支持 function calling 的端点（如魔搭 Qwen3）：
# VITE_LLM_CHAT_BASE_URL=https://api-inference.modelscope.cn/v1/chat/completions
# VITE_LLM_CHAT_API_KEY=sk-xxx
# VITE_LLM_CHAT_MODEL=Qwen/Qwen3-14B

# ── 可选：覆盖内置模型下拉列表（逗号分隔） ──────────────────────
# VITE_AGENT_MODELS=gpt-4o-mini,Qwen/Qwen3-14B,deepseek-chat
# VITE_AGENT_VISION_MODELS=gpt-4o,Qwen/Qwen3-14B
