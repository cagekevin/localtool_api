# 断点修复方案（基于三域断点梳理的可落地路线）

> 范围：汇总 `断点三域梳理.md` + `断点总表-说明补全.md` + `调试基建与探针说明.md` 的全部断点，给出**按优先级排序、可实施**的修复路线。
> 方法：每条修复回到 `localTool/src`、`apimart-gateway/main.py`、`dist/` 源码实锤，标注关联断点编号与原文档章节。
> 状态图例：`[方案]` = 待实施的设计；`[已落地]` = 已在代码中完成；`[待联调]` = 代码改完但需前端联调验证。
> 优先级铁律：**P0 > 高 > 中 > 低**（沿用原总表），且一切修复不得破坏「唯一入口」架构——所有外部流量仍经 `localTool :18080` → `/api/proxy` 转发。

---

## 〇、修复前置条件（不做则后续全白搭）

| # | 前置 | 说明 | 依据 | 状态 |
|---|---|---|---|---|
| F1 | **真实 Lovart AK/SK** | `.env` 当前是 `ak_xxxx_REPLACE_ME`/`sk_xxxx_REPLACE_ME` 占位符；`OPEN_RELAY=true` 下网关用它调 Lovart，占位符→生图/对话永远不通 | 断点三域梳理·七.6 | `[方案]` 需用户提供 |
| F2 | **端口事实对齐** | 实跑端口：`localTool :18080`、`网关 :9004`（启动脚本 `run_local.sh`/`启动网关.command` 覆盖 `main.py` 默认 `8000`）。所有硬编码 `8000` 须改为 `9004` | 本对话实测 + `启动网关.command:14` | `[已落地]` 探针已改 `9004` |
| F3 | **合并启动器** | 一键串起 localTool + 网关 + 打开画布，否则部署就绪度差（I1） | 断点总表·I1 | `[方案]` 见阶段0 |

> 注：调试面板（Ctrl+Shift+D / 🔍 按钮）、端点探针（16 端点）、proxy 结构化日志、stub 标记、`.env` 警告均已 `[已落地]`（见 `调试基建与探针说明.md`），它们是本方案的**验证工具**而非断点本身。

---

## 一、P0 修复（阻塞一切生成/对话，最高优先，三处同在 `/api/proxy`）

`system.ts` 的 `handleProxy` 是画布唯一出口，三缺陷连带让所有生成/对话失败。

### P0-1 解除 15s 硬超时 `[方案]`
- **断点**：`system.ts:67`（`handleProxyFormData`）+ `system.ts:126`（`handleProxyJson`）均 `setTimeout(() => controller.abort(), 15000)`，超时返 504。
- **修复**：超时改为可配置，读 `process.env.PROXY_TIMEOUT`（默认 `300000` 即 300s）；视频提交/流式对话等长任务不再被掐断。两处 `setTimeout` 统一替换为环境变量值。
- **验证**：提交一个 >15s 的视频任务，proxy 日志（`[proxy] ... | 504`）消失。

### P0-2 转发 SSE 流式 `[方案]`
- **断点**：`system.ts:78` + `system.ts:137` 均 `Buffer.from(await fetchRes.arrayBuffer())` 整段缓冲后 `res.end()`，不转发流；`chat/completions` 带 `stream:true` 收不到增量 → 对话假死。
- **修复**：当下游响应 `Content-Type` 含 `text/event-stream`（或请求体 `stream:true`）时，改 `res.writeHead(fetchRes.status, resHeaders)` 后直接 `Readable.fromWeb(fetchRes.body).pipe(res)` 透传流；非流式仍走原缓冲路径。
- **验证**：调试面板「请求日志」里 `chat/completions` 出现持续流入的分块响应（非一次性 200）。

### P0-3 协议翻译层 `[方案]`
- **断点**：`handleProxy`（`system.ts:24-39`）纯透传 header+body，网关返回 `{code,data}` 信封若与前端解析预期不符 → 白收到。
- **修复**：在转发返回处加轻量适配——识别 `body.code`/`body.data` 信封，按端点映射成前端形状（见 S5）；未知结构原样透传降级。
- **验证**：生图/对话返回能被前端正常解析（结合 P0-2 流式 + S5 联调）。

---

## 二、高优先修复（整块功能不可用）

### 高-1 网关补齐视频轮询 + draw 端点（G1/G2/G3）`[方案]`
- **断点**：`main.py` 缺 `GET /v1/videos/{id}`（G1）、`GET /v1/video/generations/{id}`（G2）、`POST /v1/draw/completions`（G3）；现有 `GET /v1/tasks/{id}:873`、`POST /v1/images/generations:591`。
- **修复**：
  - G1 → alias 到 `/v1/tasks/{id}`，把 `status`/`video_url` 映射成前端期望的 `status==="completed"` + `video_url/metadata.url/output.url`（断点总表·G1）。
  - G2 → 同上 alias，映射成 `status==="succeeded"` + `results[0].url`（`httpClient.js:561` 已实证该形状）。
  - G3 → 在 `/v1/images/generations` 基础上加 `/v1/draw/completions` 接受 `{model,prompt,aspectRatio,urls}`，复用绘图提交逻辑。
- **验证**：探针 gw-videos-poll / 重跑 draw 节点返回 200 且字段吻合。

### 高-2 画布页面宿主 + 自动开浏览器（L2）`[方案]`
- **断点**：`index.ts` 静态服务只处理 `/files/*`（`index.ts:45-97`），无托管 `dist/index.html`、无 `open()` 浏览器。
- **修复**：localTool 增加一个根路由 `/` 与 `/index.html` 托管 `localTool/../dist/index.html`（方式一同源）；启动后 `open('http://127.0.0.1:18080')` 自开浏览器（Mac `open` / Win `start`）。
- **验证**：双击启动 → 自动弹出画布页面。

### 高-3 合并启动器（I1，F3）`[方案]`
- **断点**：根目录无脚本把 localTool(:18080)+网关(:9004)+打开画布串起来。
- **修复**：根目录新建 `launch-all.sh`/`launch-all.command`，顺序：① 起 localTool（`localTool/start.sh`）② 起网关（`apimart-gateway/启动网关.command`，已 chmod +x）③ 打开 `http://127.0.0.1:18080`。"启动网关.command" 权限已授予（`-rwxr-xr-x`）。
- **验证**：一键启动后两个服务在线、画布可进。

### 高-4 `sync/default` 本地兜底（A2/L4）`[方案]`
- **断点**：`/api/sync/default` 是画布拿到 apiConfig 的**唯一来源**，官方不可达→节点库空白；`index.ts` 路由表无此路径。
- **修复**：localTool 实现 `GET /api/sync/default`，返回基线 `apiConfigs`（节点/模型默认目录，原梳理·七.1 已定性为"本地默认配置"），把其中的 `{VITE_API_BASE_URL}` 占位符替换为 `http://127.0.0.1:18080`。
- **验证**：官方离线时节点库仍有默认节点/模型；探针可加 `sync-default` 由 warn(404) 转为 ok。

---

## 三、中优先修复

| # | 断点 | 修复思路 | 文件:行号 | 状态 |
|---|---|---|---|---|
| 中-1 | L1 `kv/delete` 未实现 | 加 `POST /api/kv/delete?key=`，复用 kv 存储删 key | `index.ts:129-134` | `[方案]` |
| 中-2 | L4/L5 `assets/upload`、`upload/app-asset` | localTool 实现 `/api/files/upload` 别名，返 `{success:true,data:{url}}`（原梳理·七.1 已定性） | `index.ts` 路由表 | `[方案]` |
| 中-3 | S1 相对路径破图（方式二） | `/api/files/upload`、`/api/files/thumbnail` 返回**绝对路径** `http://127.0.0.1:18080/files/...`，避免 `chrome-extension://` 下 404 | `files.ts:40-47,234` | `[方案]` |
| 中-4 | P2 ffmpeg 未审计 | 确认原版是否内置 ffmpeg、前端后处理是否依赖；若需，localTool 集成 ffmpeg 并 `handleStatus` 报 `ffmpeg:true`（`system.ts:18`） | `system.ts:18` | `[方案·待定]` |
| 中-5 | I3 未预编译 | 确认 `start.sh` 含 `npm run build`；或提交 `localTool/dist/` 产物开箱即用 | `localTool/` 无 dist/ | `[方案]` |
| 中-6 | G4 `assets/upload` 缺失 | 网关补 `POST /api/assets/upload`（Bearer+JSON）或 alias 到 `/v1/uploads/images` | `main.py` | `[方案]` |
| 中-7 | G5 `ai-app/*` 缺失 | 仅当启用 AI 应用市场时实现 `/v1/gateway/ai-app/*` + base 级 `/api/ai-apps`；核心生图/视频不依赖，可暂缓 | `main.py` | `[方案·可选]` |
| 中-8 | A1/A3/A4 官方无兜底 | 把助手 `vip-check`/`chat`、工作流 `workflow-apps`、资产 `app-asset` 重定向到 localTool 兜底或官方（原梳理·七.1 定性） | 前端 `endpointConfig.js` | `[方案]` |

---

## 四、低优先修复（不影响核心生图/视频/对话）

| # | 断点 | 修复思路 | 文件:行号 | 状态 |
|---|---|---|---|---|
| 低-1 | L3 版本残留 | `platform.ts:13` 与 `index.ts:24` 版本统一（建议统一为真实版本号） | `platform.ts:13`/`index.ts:24` | `[方案]` |
| 低-2 | S2 缩略图降级 | 有 `sharp` 时生成真缩略图，无则保持 `copyFileSync` 降级 | `files.ts:120,230` | `[方案]` |
| 低-3 | S3/S4 字段名联调 | 联调 `/api/tasks`、`/api/resources`（`{items,...}` vs `list/data`）、`/api/kv/get`（裸值 vs `{value}`）与前端取字段一致性 | `helpers.ts:237`/`kv.ts:16-19` | `[待联调]` |
| 低-4 | I4/C1 安全 | 开放局域网时 `OPEN_RELAY` 加鉴权、CORS 收紧为 `127.0.0.1`，勿绑 `0.0.0.0` | `index.ts:106`/`main.py:33` | `[方案]` |
| 低-5 | G6 听音 501 | 外接第三方语音转写，或置灰「听音」入口 | `main.py:667-669` | `[方案]` |
| 低-6 | S5 task_id 取法 | 网关 `ok([{status,task_id}])`（`main.py:781`）→ 确认前端 `data[0].task_id` 取法一致 | `main.py:781` | `[待联调]` |
| 低-7 | A5 视频基址官指向 | `video`/`sd2Video` 节点基址默认 `ai-api.kkidc.com` 重定向到 localTool 走统一中继 | `httpClient.js:561` | `[方案]` |
| 低-8 | A6 硬编码 fallback | 离线/内网环境去残留公网 fallback（`154.219.102.152:3012` 等） | `endpointConfig.js` | `[方案]` |
| 低-9 | A7 O5 `0.1mao.cc` | 确认 `Dn` 默认占位用途（原梳理判非关键），自托管可改为 `http://127.0.0.1:18080` | `httpClient.js`/`App.js` | `[方案·待确认]` |

> 伪覆盖端点（`/v1/music/generations` 等 501，断点总表·伪覆盖）属非契约，不计入修复，真用音乐/语音须节点单独配第三方（如 kkidc）。

---

## 五、分阶段实施路线（建议顺序）

```
阶段0  前置：F1(真实AK/SK) → F2(端口9004已对齐) → F3(合并启动器 I1)
  ↓
阶段1  P0 三件套：解除超时 / 流式透传 / 翻译层   —— 让"生成/对话"至少能跑通
  ↓
阶段2  高-4 A2 sync/default 兜底                —— 节点库有默认配置，画布不空白
  ↓
阶段3  高-1 网关 G1/G2/G3 + 高-2 L2 宿主 + 高-3 I1 启动器
  ↓
阶段4  中-1~中-8 本地缺口（kv/delete、assets、S1 绝对路径、ffmpeg、预编译、G4/G5、A1/A3/A4）
  ↓
阶段5  低-1~低-9 + 联调验证（S3/S4/S5 字段对齐）
```

每阶段完成用「调试面板 + 端点探针（16 端点）+ proxy 日志」回归验证，详见第六节。

---

## 六、验证方法（复用已落地的调试基建）

1. **端点探针**：重载扩展 → 侧边栏 🔍 → ▶ 探针。重点看：
   - P0 修复后 gw-*（16 端点中的 5 个）由 NET_ERR/404 转为 ok 或暴露真实 G1/G2/G3 缺口；
   - 高-4 后 `sync-default` 由 warn(404) 转 ok。
2. **请求日志 Tab**：过滤 `localhost|127.0.0.1|9004`，观察 `chat/completions` 是否流式分块（P0-2 验证）。
3. **proxy 结构化日志**：localTool 终端 `[proxy] ... | <status> | <ms>`（`system.ts:81/145`），看超时/错误。
4. **`.env` 启动警告**：localTool 启动若仍报占位符警告 → F1 未解决。
5. **stub 标记**：`workflow-by-proj` 等带 `_meta.stub` 的响应在探针里标 `📌 stub`，联调后可移除标记。

---

## 七、与三份文档的对应表

| 本文章节 | 源文档依据 |
|---|---|
| 〇 F1 | 断点三域梳理·七.6（`.env` 占位符硬前置） |
| 〇 F2 | 本对话实测 + `启动网关.command:14`（端口 9004） |
| 一 P0-1/2/3 | 断点三域梳理·B.4 + 断点总表·P0 三条（`system.ts:67/78/126/137`） |
| 二 高-1 | 断点总表·G1/G2/G3（`main.py:873/591`） |
| 二 高-2/3 | 断点总表·L2 / I1（`index.ts:45-97`） |
| 二 高-4 | 断点三域梳理·七.1（A2 定性 localTool） |
| 三 中-1~8 | 断点总表·L1/L4/L5/S1/P2/I3/G4/G5/A1/A3/A4 |
| 四 低-1~9 | 断点总表·L3/S2/S3/S4/I4/G6/S5/A5/A6/A7 |
| 五~六 | 调试基建与探针说明（探针 16 端点 / 日志 / 警告） |

> 本文只给方案与落地优先级，**不含最终代码实现**；实施时按阶段推进，每阶段用第六节工具回归。P0 三件套是解锁一切生成/对话的钥匙，建议最先动手。

---

## 八、安全操作与回退（防改错）

> 状态：本工作区已 `git init` 纳入版本管理，首次提交为「改动前可跑版本」基线快照。所有修复均可在改错后 `git checkout -- <文件>` 秒回。localTool 为 TypeScript 源码（`build = tsc`），改 `src/*.ts` 须 `npm run build` 生成 `dist/index.js`；网关 `main.py` 为单文件 Python，改完直接重启 uvicorn。

### 8.1 版本管理（回退保险）
- 已建 git 仓库；每次改完**一个断点**就 `git add -A && git commit -m "fix: xxx"`，形成可回退节点链。
- 改错回退：`git checkout -- localTool/src/routes/system.ts`（单文件回到上次提交）；或 `git revert <commit>` 回退整次改动。
- 未建 git 时的兜底：改前 `cp <file> <file>.bak`，恢复时 `.bak` 覆盖回去再 build/重启。

### 8.2 单点修改协议
1. **一次只改一个函数/一个断点**（如只动 `system.ts:67` 超时），不攒一堆一起改。
2. 改完先 `npm run build`（tsc 类型检查）——语法/类型错在此拦下，且 **build 没成功不会覆盖 `dist/`，原服务不受影响**，不慌。
3. localTool 改 `src/*.ts` → `npm run build` → 重启 `node dist/index.js`（或 `start.sh`）；扩展在 `chrome://extensions` 点「重新加载」。
4. 立刻跑端点探针（🔍 → ▶ 探针）确认这一改没把别的搞坏。

### 8.3 救援清单
| 症状 | 原因 | 救法 |
|---|---|---|
| `tsc` 报错 | 类型/语法错 | 按红字改；未 build 成功不覆盖 dist，原服务仍跑 |
| build 过但功能坏 | 运行期逻辑错 | `git checkout` 还原 `src` → 重新 `npm run build` → 重启 |
| 网关启动报错 | `main.py` 逻辑错 | 还原 `main.py` → 重启 uvicorn |
| 扩展面板异常 | `background.js`/`debug-panel.js` 写错 | 调试基建坏了不影响主功能；还原备份或 reload 旧版 |
| 全乱了 | 多处改崩 | 所有 `src`+`main.py` 还原基线 → `npm run build` → 重启 → 回到可跑状态 |

### 8.4 三层验证（确认真修好，不是假象）
1. **连通层**：探针 ok（网关/接口可达）。
2. **解析层**：请求日志响应字段正确（非白收到）。
3. **功能层**：实际生图/生视频/对话成功产出。
三层均过才算修好；对照第六节工具（探针 / 请求日志 / `[proxy]` 日志 / `.env` 警告）。
