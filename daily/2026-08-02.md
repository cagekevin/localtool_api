# 修复日志 — 2026-08-02

> 今日工作：src/bundle base 改造落地方向一/五/六 + 修复 smoke_test 质量门 + localTool 出站代理支持。

---

## 一、src/bundle base 改指 18080（方向一/五/六 落地）

提交 `8beec3e` `src/bundle 改动：base 指向 18080 落地方向一/五/六（build 回灌 dist）`

- `src/bundle/endpointConfig-Bt85xi8d.js`：`s()` 候选列表首项插入 `{"label":"本地引擎","url":"http://127.0.0.1:18080"}`（置顶 → `c[0].url`=18080）、`l()` 回退常量改 `http://127.0.0.1:18080`，两处加 `// [base→18080]` 注释。
- `npm run build` 回灌 `dist/`，产物 grep 确认 `c[0].url`=18080。
- `docs/01` 变更#1 台账更新为「已落实/已回灌 dist」。
- 影响：方向一（权益转发）、方向五（内置模型）、方向六（A1 重定向）五处接口由直连官方改为经 localTool 转发/本地兜底；未接管请求走 catch-all 透传官方。
- 真机走查（用户提供日志）：`/api/user/info`、`/api/user/model-entitlements`、`/api/agent/canvas-assistant/vip-check` 全 200（含缓存 HIT）；`/public/platform/builtin`、`/public/platform/models`、`/api/sync/default`、`/plugin/manifest.json`、`/api/workflow-apps/by-project/default` 全部落 18080。

## 二、修复 smoke_test 质量门误报（脚本 bug，非代码问题）

提交 `ecb1033` `fix(scripts): 修复 smoke_test 质量门误报，恢复硬断言能力`

三个 FAIL 项根因均为 `scripts/_smoke_checks.cjs` 自身缺陷：
1. `checkDistAssets`：子目录 HTML（`share/index.html`）的 `../assets/...` 引用基准错位（跑 `dist/../assets`）→ 按 HTML 所在目录解析。
2. `checkImportGraph`：只 `readdirSync` 顶层 `.js`，子目录 `*_components/` 的 chunk（如 `shared.js`）被误判缺失 → 递归收集 `src/bundle/**/*.js` 按相对路径比对。
3. `checkReadableParity`：`readable/` 不存在且提示的 `npm run readable` 命令不存在 → 缺失时自动调 `rename.cjs` 自愈；`rename.cjs` 改为递归复制子目录。

配套：`.gitignore` 新增 `readable/`（阅读副本、可重建、不入库）。修复后 `node scripts/smoke_test.cjs` **ALL PASS**。

## 三、localTool 出站代理支持（跨平台，零依赖）

**现象**（用户真机走查日志）：`POST /api/files/upload 400` 反复出现，`Failed to sync to local tool: Error: Upload failed`。画布加载时批量转存历史任务的 Lovart 图（`a.lovart.ai/artifacts/agent/*.png`）。

**根因**（实测确认）：
- `a.lovart.ai`（Lovart CDN）在本机**必须经代理 `127.0.0.1:7897` 才能访问**（直连超时 `000`，走代理 `200`）。
- 浏览器配了代理能开；但 **localTool 是独立 Node 进程，原生 fetch 不继承浏览器/系统代理** → `saveRemoteUrl` 里 `fetch(a.lovart.ai)` 超时 → 400。
- 归属：**localTool** `/api/files/upload`（第 3 类职责"上传/资产"）。非前端/网关/官方问题。

**方案**（最优解，满足 CLAUDE.md §3 奥卡姆剃刀，零第三方依赖）：
- 新建 `localTool/src/utils/netProxy.ts` 的 `fetchWithProxy`：
  - 直连优先（本地 `127.0.0.1`/`localhost`/内网、公网直连可达的最优路径）；
  - 直连失败 → 读代理环境变量（`HTTPS_PROXY`>`HTTP_PROXY`>`ALL_PROXY`，大小写兼容）→ 无则探测本机常见代理端口（`7897/7890/1087/1080/8888/8118`）；
  - 用 `node:https` + `tls.connect({socket, servername})` 做 CONNECT 隧道重试。
- `files.ts`：`saveRemoteUrl` 下载改走 `fetchWithProxy`；`handleReadProxy`（`x-proxy-url` 外部代理读）同改。
- `/api/proxy`→`:9004`（本地）、`official.ts`/`passthrough.ts`→官方（直连可达）**不涉及**，保持最小改动。

**实现要点（踩坑记录）**：CONNECT 隧道建立后必须 `tls.connect({ socket })` 包一层 TLS 再发目标请求，否则报「The plain HTTP request was sent to HTTPS port」(400)。`ProxyResponse` 不做 `implements Response`（TS 对 `bytes()` 返回泛型过严），用结构兼容 + `as unknown as Response`。

**验证**（实测通过）：
- 直连失败→代理重试：`fetchWithProxy('https://a.lovart.ai/...')` 返回 **200，3212185 bytes**（与 curl 走代理一致）。
- 端到端 `POST /api/files/upload -F fileUrl=a.lovart.ai... -F subfolder=tasks` → 返回 `{url, path, thumbnailUrl}`，文件 + 缩略图落盘 `~/.maomao-localtool/uploads/tasks/`。

**CLAUDE.md**：§三.2 本地落盘机制补「出站代理」条目（登记本机制与适用点）。

---

## 待办（未做/需决策）
- 真机走查完整回归（本次 localTool 代理修复后，历史任务图转存应不再 400）。
- 方向二（特惠视频第三方）：待业务决策（路线 A 保留官方特惠 vs 路线 B 自研接管）。
- 方向三（剪映发送）/四（工作流应用市场）：localTool stub，待实现。
- `BUILTIN_MODELS` 空字段（`discountVideoSpecs`/`power`/`unit`/`descriptions`）补数据（可选后补）。
