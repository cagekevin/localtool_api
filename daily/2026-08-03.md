# 修复日志 — 2026-08-03

> 今日工作：React 双实例 / vendor 死重修复（方案 S1）真机验证闭环 + 登记 + 遗留 Bug B 梳理。

---

## 一、React 双实例 / vendor 死重修复（方案 S1）真机验证闭环

**现象（历史）**：真机打开带 Three.js 的画布页崩溃：
```
THREE.WARNING: Multiple instances of Three.js being imported.
TypeError: Cannot read properties of null (reading 'useMemo')
    at e.useMemo (vendor-Z-adA07W2.js:1:7323)
    at lg (App-BX6o9fW5.js:31:54230)
    at fi (vendor-Z-adA07W.js:8:48278)
    ...
```

**根因（§2.2 机制 + 本次确认）**：
- 源 `index.html` / `share/index.html` / `public/share/index.html` 各有一行 `<link rel="modulepreload" ... vendor-Z-adA07W.js>`。
- 该 preload 把 `vendor-Z-adA07W.js` 变成 **entry 身份**，占用输出名 `vendor-Z-adA07W`；`vite.config.ts:79` 的 `manualChunks` 又对同名源文件强制命名 `vendor-Z-adA07W` → Rollup 命名冲突 → 被 import 的副本被迫追加 `2` 后缀 → 产出 `vendor-Z-adA07W2.js`。
- 于是 `main-CYvt_zul.js` 用**无2版 vendor** 的 react-dom 渲染 App，而 App 的 hooks（经 `_react_shim`）静态改写后解析到 **`adA07W2` 的 React** → 运行时双 React 实例、dispatcher 为 null → `useMemo` 崩溃。

**修复（方案 S1，已实施）**：删除三处 HTML 的 vendor `modulepreload` 行（各删 1 行，共 3 行）：
- `index.html`
- `share/index.html`
- `public/share/index.html`

**静态验证（通过）**：`npm run build` 干净重建后 `dist/assets/` 只剩 **1 个** `vendor-Z-adA07W.js`，`adA07W2` 消失；全部 8 个业务模块（`main`/`share`/`App`/`ShareAppPage`/`_react_shim`/`_jsx_runtime`/`httpClient`/`mediabunny`）React 符号 100% 指向同一份 vendor；`npm run test:smoke` **ALL PASS**（含 `dist 重复 chunk  PASS`）。

**真机验收（✅ 通过）**：打开带 Three.js 的画布页，`useMemo` 崩溃**消失**、`Multiple instances of Three.js` 警告**消失**。真机报错栈消失，闭环达成。

**登记**：`docs/22-React双实例与vendor死重排查报告.md` 顶部新增「✅ 修复已真机验证闭环（2026-08-03）」状态块；§九 新增「试错 #3 · 成功」；旧「试错 #1」S1 失败结论标注已被复测推翻。

> **关键避坑（重要）**：真机测试前必须用最新源码 `npm run build` 干净重建 `dist/`，并确认 `dist/assets/` 无陈旧 `adA07W2.js` 残留。此前「试错 #1」误判 S1 失败，极可能正是测了陈旧产物（`adA07W2` 仅存在于旧产物）。

---

## 二、遗留 Bug B 说明（非本次修复范围，另立跟踪）

**Bug B 是什么**：`dist/share/index.html` 同时注入 `main-CYvt_zul.js` 与 `share-CyPsaet6.js` 两个入口脚本。

**成因**：`main` 与 `share` 两个 entry 各自源码都定义了同名的 `__vite__mapDeps` 辅助函数，Rollup 在构建期将其提取为共享代码、归属到 `main`，再让 `share` 反向 `import main` → 分享页把主画布应用和分享页两个入口**同时挂载到同一个 `#root`**。已实测 `main-CYvt_zul.js` 与 `share-CyPsaet6.js` 各含 1 处 `createRoot`，即分享页会跑**两次 `createRoot`**。

**影响**：双 `createRoot` 并发渲染同一 `#root` 会互相打断渲染阶段，属独立于 React 双实例的第二个潜在崩溃源（docs/22 §2.6 Bug B / 方向 C）。

**✅ 已于同日修复并真机验证通过**（见下文「三、Bug B 修复」）。

---

## 三、Bug B 修复（分享页双 createRoot）真机验证闭环

**成因（§2.6 Bug B）**：main/share 共享 `__vitePreload` 辅助，Rollup 把它归到 main 并让 share 反向 `import main` → 分享页同时加载 `main-CYvt_zul.js` 与 `share-CyPsaet6.js` 两个入口，跑两次 `createRoot` 挂同一 `#root`。注：`share → main` 的 import **仅存在于构建产物**，源码中没有（原方案改 share import 不可执行）。

**修复（产物层兜底，`vite.config.ts` `post-build-fixups` 新增 ④ 步骤）**：
- 从 `dist/share/index.html` 剔除 `main-CYvt_zul.js` 的 `<script>` 注入；
- 从 `dist/assets/share-CyPsaet6.js` 剥离对 main 的 `import`，并将内层空依赖 `__vitePreload` 包裹（`[]` 无预载）退化为直接调用（等值）。

**先尝试但放弃**：源码给 `share-CyPsaet6.js` 的 `__vite__mapDeps` 改名（方案 2）——实测 Vite 仍让 share import main（共享的是 `__vitePreload` 而非 mapDeps，Vite 自动注入、源码改名无效），已 `git checkout` 回退，改走产物层兜底。

**静态验证（通过）**：`dist/share/index.html` 只加载 `share-CyPsaet6.js`、不再注入 main；`share-CyPsaet6.js` 不再 import main、含 1 个 `createRoot`；`dist/index.html` 正常含 main 的 1 个 `createRoot`；`npm run test:smoke` **ALL PASS**。

**真机验收（✅ 通过）**：分享页正常显示、不再双挂载。

---

## 四、AI 操控画布后端落地（docs/27 实施，选项 A）

**背景**：官方 A1 画布助手前端黑盒已内置完整闭环（30 画布工具 + Agent 多轮循环 + 浏览器端执行器 `lr()`），后端只缺一个「支持 function calling 的 LLM 中转端点」。docs/27 定稿方案实施。

**改动**：
- 新建 `localTool/src/routes/agentChat.ts`：`POST /api/agent/:id/chat` SSE 透传处理器。配置全量走 `.env`（`LLM_CHAT_BASE_URL/API_KEY/MODEL` 等，换 LLM 零代码改动）；复用 `fetchWithProxy` 支持外部 LLM 走本机代理；SSE/gzip/非 SSE 兜底齐全。
- `localTool/src/index.ts`：具名区注册 `/api/agent/:id/chat` + 启动 `loadDotEnv()` 读 `.env`。
- `localTool/src/routes/official.ts`：vip-check 对 `canvas-assistant` 本地放行。
- 新建 `localTool/.env`（gitignored，含魔搭 Key）+ `.env.example`（入库模板）。

**LLM 选型（实测）**：走**魔搭 ModelScope**（选项 A）。实测关键结论：
- 端点 `api-inference.modelscope.cn` 在本机**直连被拒**，需走 `127.0.0.1:7897` 代理（`fetchWithProxy` 自动处理）。
- `Qwen/Qwen2.5-7B-Instruct` 不可用（`has no provider supported`）；**`Qwen/Qwen3-14B`** 可用且返回 `tool_calls`。
- 已写入 `.env`：`LLM_CHAT_MODEL=Qwen/Qwen3-14B`。

**端到端验证（✅ 通过）**：localTool 读 `.env` → 走代理 → 魔搭 `Qwen3-14B` → SSE 透传，返回 **80 行 `tool_calls` delta** + `finish_reason:"tool_calls"` + `[DONE]`，前端可解析并驱动画布操作。

**已知影响**：因走代理，SSE 流被缓冲成一次性返回 → **打字机效果退化**（用户接受），tool_calls 多轮循环与画布操作功能正确。

**待真机验证（盲点，docs/27 §10.2/§11.6）**：LLM 返回 tool_calls 后，前端画布句柄 `r.current` 在本地引擎模式下是否就绪——若画布节点不动且报「画布句柄未就绪」，是前端运行时问题，与后端路由无关。

---

## 提交

- **提交 1（`1858d14`）**：React 双实例/vendor 死重修复（S1）+ docs/22 + daily 初稿。
- **提交 2**：Bug B 修复（`vite.config.ts` post-build-fixups ④）+ docs/22 + daily 更新。
- **提交 3（本次）**：AI 操控画布后端落地（docs/27 选项 A，魔搭 Qwen3-14B）+ docs/27 + daily。
- `dist/` 为 gitignored 构建产物，不入库；`localTool/.env` 为 gitignored 密钥配置，不入库。
