# 修复日志 — 2026-08-07

> 今日工作：① 排查 localTool 是否还有"不支持中文"同类低级错误（结论：核心链路已全通，无崩溃级问题）；② 系统性排查 localTool 潜在隐患，按**奥卡姆剃刀**决策——均属"目前未暴露"的低概率风险，**不做代码改动，仅登记留痕**。

---

## 一、localTool 潜在风险排查（奥卡姆剃刀决策：暂不处理）

按用户要求，对 localTool 全部核心文件（index 路由、admin/files/kv/official/passthrough/platform/resources/system/tasks 路由、database、helpers、netProxy、fileStore）做了隐患扫描，发现若干"目前未暴露"的潜在风险点。

**决策原则（奥卡姆剃刀）**：这些都不算当前暴露的大问题，若为它们引入鉴权机制/复杂加固等新实体，反而增加复杂度与回归面。**故不写代码，仅登记留痕**，后续若实际触发再针对性处理。

### 潜在风险清单（已识别，暂不修复）

| # | 位置 | 风险描述 | 严重度 | 处理 |
|---|---|---|---|---|
| 1 | `resources.ts` `resourceToRow`/`upsertResource` | 无列名白名单（tasks.ts 有 `ALLOWED_TASK_COLUMNS`，resources 漏了），前端 body 键拼 SQL 列名 | 🔴 中（仅本机+固定字段，触发面窄） | 暂不修 |
| 2 | `helpers.ts` `buildPaginatedQuery` L195 | `filters` key 拼列名无白名单（值已参数化） | 🔴 中 | 暂不修 |
| 3 | `admin.ts` `handleAdminImport` L167-179 | 导入数据键拼列名无白名单 | 🔴 中 | 暂不修 |
| 4 | 管理接口无鉴权 + CORS `*` | 本机恶意网页可 CSRF 触发 export/import/cleanup/clear-cache（服务仅绑 127.0.0.1） | 🟡 中 | 暂不修（加鉴权=新增实体，违反剃刀） |
| 5 | `admin.ts` `cleanup` / `database.ts` `deleteLocalFile` | URL 编码一致性依赖"DB 存原始中文 URL"；若存编码 URL 会误删/删不掉 | 🟡 中 | 暂不修（当前假设成立，实测正常） |
| 6 | `helpers.ts` `parseMultipart` L86/L97 | 硬编码 CRLF，非标准客户端可能损坏文件 | 🟢 低 | 暂不修（浏览器恒 CRLF） |
| 7 | `admin.ts` import 无规模/类型校验 | 可导入畸形数据清空业务库（需 confirm:true） | 🟡 中 | 暂不修 |
| 8 | `netProxy.ts` `proxyResponse.clone` | clone 后 body 一次性空流 | 🟢 低 | 暂不修 |

> 注：若后续某项从"潜在"变为"实际暴露"，按优先级处理——先 1/2/3（列名白名单，改动最小、照抄 tasks.ts 现成模式），再考虑管理接口鉴权。

---

## 二、复盘：剧本盒子中文路径修复（昨日延续）

昨日已修复剧本盒子「生成资产」两个叠加 bug 并推送（`20dd0d2`/`f3c23f6`/`bb340b3`）：
1. `Mr` 不解析 SSE → 兼容 SSE 流解析（前端）。
2. localTool 静态服务 `handleStaticFile` 不解码中文 subfolder → 加 `decodeURIComponent`。

**今日补充排查**：确认 localTool 其余文件/路由对中文均正常（`handleThumbnail`/`handleRead`/`handleList`/`handleMkdir`/`handleOpen`/`handleOpenDir` 走 `searchParams.get` 自动解码或 JSON body 原样，直接 `path.join` 落盘，实测 thumbnail/read 均 200）。唯一用未解码 `pathname` 拼磁盘路径的 `handleStaticFile` 已修，是根上的断裂点，已覆盖所有 `/files/` 静态读取。

---

## 三、测试残留清理

排查实测时产生的 `~/.maomao-localtool/uploads/测试目录/` 已清理（rm -rf），无残留。
