# 修复日志 — 2026-08-11

> 今日工作：**修复剧本盒子「编辑已生成提示词」整页白屏崩溃** —— 定位根因（`c_.jsx` 原地 contentEditable 受控编辑 + 每按键全量重渲染 + React 重排编辑中 DOM）→ 写排查文档 `docs/38` → 实施「双击→弹窗→保存写回」根治改造 → `npm run build` 回灌 dist → 在 `docs/01` 登记变更 #15。

---

## 一、剧本盒子「编辑已生成提示词」整页白屏崩溃 —— 根因定位

### 现象

- 剧本盒子（`scriptBoxNode`）步骤3「合成提示词」，**编辑已经由 AI 生成的提示词**（single 视图「生图」`prompt` /「生视频」`videoPrompt`，以及 `description`）时，整页白屏（React 整树卸载）。
- 与接口/网关/VPN 无关，纯前端运行时崩溃。

### 根因（源码逐点核对，`src/bundle/httpClient-BknZwXjG_components/c_.jsx`）

分镜字段编辑器 `rt()`（c_.jsx:546）用 **`contentEditable` 内放 React 受控子节点**（`_cmp_a_` 按 `@资产名` 拆成 string+`<span>` 混合数组），叠加两个放大因素，缺一不可：

| # | 因素 | 代码落点 |
|---|---|---|
| ①（原罪） | `contentEditable` 内放 React 受控子节点，浏览器编辑态与 React 虚拟 DOM 双源冲突 | `rt()` L546-606 + `a_.jsx` |
| ② | 每次按键 `onInput` → `He()` → `updateNodeData` → data.shots 新引用 → 整个 2805 行组件全量重渲染 → React 重排该 div 内部 | `rt()` `onInput` → `He()` L231 |
| ③（扳机） | `onKeyDown` 里 `@资产名` 联想/删除直接写 `r.innerText`（`nt`/`go`/`_o`），与 React 重排叠加 | `nt()` L496-543 |

一次按键链路：`onInput → updateNodeData → 全组件重渲染 → React 对"正在被浏览器编辑中的 contentEditable 内部 DOM"做 reconciliation → DOM 突变冲突抛错 → 无 Error Boundary → React 卸载整树 → 白屏`。空字段子节点极简相对安全；内容一变长/含 `@资产`/在 `@` 附近删改即必崩。

### 涉及字段与代码入口

`rt()` 三处调用（全部是可点击即编辑的 contentEditable）：list 视图 L757 `description`、single 视图 L2083 `description`/L2102 `prompt`/L2106 `videoPrompt`。

> 完整排查文档：`docs/38-剧本盒子编辑已生成提示词崩溃排查.md`（含三方案对比、落地对照、重打提示）。

---

## 二、实施「双击→弹窗→保存写回」根治改造（变更 #15）

### 方案

把分镜字段从「原地 contentEditable 编辑」改为「**双击 → 独立弹窗用受控 textarea 编辑 → 保存一次性写回原字段**」。编辑态彻底脱离 React 重排区，从根上消灭 ①+②+③。

### 代码改动（仅 `src/bundle/httpClient-BknZwXjG_components/c_.jsx`）

1. **`rt()` 改只读展示 + 双击**：`<div role="textbox" contentEditable onInput onKeyDown> + @资产 联想浮层` → **只读 `<div>`**（`role="button" title="双击编辑"`，仍用 `_cmp_a_` 高亮），`onDoubleClick` → `ems({shotId, field, value})`。**移除** `onInput`/`onKeyDown`/`@` 联想浮层。三处调用点零改动，自动跟随。
2. **新增编辑弹窗**：组件内加 `let [em, ems] = Z.useState(null)` + `let emRef = Z.useRef(null)`。组件结尾加 `em && Fn.createPortal(...)` 弹窗：受控 `<textarea>`（`value=em.value`，onChange 只改 `em.value`，**保存前不碰 node.data**）、资产名插入按钮（光标处插 `@资产名 `，textContent 拼接）、「保存 (Ctrl+Enter)/Esc」→ `He(shotId,{field:value})` 一次写回并关闭。

### 验证

- `npm run build`（Vite 回灌 dist）成功：`✓ built in 6.86s`，`197 modules transformed`。
- dist 确认含「双击编辑」「保存 (Ctrl+Enter)」「点资产名插入」。
- `test:smoke` 的 React 单实例 / chunk 引用 / dist 重复 chunk **全 PASS**。

### ⚠️ 既有问题（与本次改动无关，需注意）

- `test:smoke` 的 `port_18080` 契约漂移 **FAIL**：`localTool/src/routes/resources.ts` 基线1→当前2。该文件本次**未触碰**（git status 无此文件），属**既有契约基线漂移**。若后续 push 需先 `npm run contracts -- --resnap` 重建基线或核对那个文件。
- 环境有**几处 pre-existing 改动**（`apimart-gateway/main.py`、`README.md`、`localTool/pnpm-lock.yaml`、`tools/` 等），均非本次改动，来自工作区之前状态。
- `npm run build` 有若干 `Illegal reassignment of import "_shared"` 告警（`H_.jsx`/`kl.jsx`/`xs.jsx` 等既有文件），**非阻断**，build 完成生成 dist。

---

## 三、文档登记

- `docs/38-剧本盒子编辑已生成提示词崩溃排查.md`（新增）：现象/复现路径/根因三因素/三方案对比（A 双击弹窗根治、B 止血、C ErrorBoundary 兜底）/落地对照。
- `docs/01 长期目标-未来适配方向.md`：登记**变更 #15**（含改动点、验证、重打提示），更新顶部最新进度。

---

## 四、待办 / 真机验证

- [ ] 用户重启画布页面加载新 dist。
- [ ] 真机走查：剧本盒子 → 步骤3 → 单镜头 → 生成提示词 → **双击**「生图/生视频」框 → 弹窗内编辑含 `@资产` 文本 → Ctrl+Enter 保存 → 确认不白屏、原位更新。
- [ ] 连线到下游生图，确认仍自动带参考图（`@资产` 收集走 `Fa` 纯字符串匹配，与 UI 无关，预期不受影响）。
