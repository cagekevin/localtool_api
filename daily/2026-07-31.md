# 修复日志 — 2026-07-31

## 架构

```
浏览器/画布 ──唯一入口──> localTool :18080 /api/proxy
                              │  转发 + 协议翻译
                              └──> apimart-gateway :9004 → Lovart
```

- 一切外部流量经 localTool `/api/proxy`，不得绕过
- 网关只 alias/透传 Lovart 原生响应，不拆信封
- 协议翻译集中在 localTool `system.ts`

## 改动文件总览

| 文件 | 涉及断点 | 行数变化 |
|---|---|---|
| `localTool/src/routes/system.ts` | P0-1~4 | ~170 行新增 |
| `localTool/src/index.ts` | L1/L2/L3/L5/A2 | ~120 行新增 |
| `localTool/src/routes/kv.ts` | L1 | +10 行 |
| `localTool/src/routes/files.ts` | S1 | +7 行 |
| `localTool/src/routes/platform.ts` | L3 | 1 行 |
| `apimart-gateway/main.py` | G1/G2/G3 | +30 行 |
| `localTool/data/apiConfigs.baseline.json` | A2 | 新建 |
| `launch-all.command` | I1 | 新建 |

---

## P0-1：解除 15s 硬超时

- **文件**：`system.ts:11`、`:71`、`:162`、`:99`、`:219`
- **问题**：`handleProxyFormData` 和 `handleProxyJson` 均 `setTimeout(() => controller.abort(), 15000)`
- **影响**：视频生成、长对话 >15s 被掐断 → 504
- **修复**：
  ```typescript
  const PROXY_TIMEOUT_MS = Number(process.env.PROXY_TIMEOUT) || 300000; // 默认 5min
  setTimeout(() => controller.abort(), PROXY_TIMEOUT_MS)
  ```
- **超时错误消息**同步改为 `Proxy request timed out (${PROXY_TIMEOUT_MS/1000}s)`
- **验证**：`PROXY_TIMEOUT=5000` 启动 → 超时 5s 触发（环境变量可覆盖）

---

## P0-2：SSE 流式转发（3 次迭代）

### v1：SSE 管道直传 + zlib 解压
- **文件**：`system.ts` `handleProxyFormData` 和 `handleProxyJson`
- **新增 import**：`Readable`(stream)、`createGunzip/createInflate/createBrotliDecompress`(zlib)
- **逻辑**：检测下游 `Content-Type: text/event-stream` → `writeHead` → `Readable.fromWeb(fetchRes.body)` → zlib 解压 → `pipe(res)`
- **问题**：`skipHeaders` 去掉 `content-encoding` 但没本地解压（v1 补了 zlib 解压后 pipe）

### v2：过滤 `: heartbeat` + 保留 `data:` 前缀
- **问题发现**：网关返回 `: heartbeat\n\n` 行，直接 pipe 给前端 → 前端 `JSON.parse(": heartbeat")` 报 `Unexpected token ':'`
- **前端解析代码**（`httpClient-BknZwXjG.js` 位置 951968）：
  ```javascript
  for(let e of s) {
      let t = e.trim()
      if(!t || !t.startsWith('data:')) continue  // ← 关键！
      let n = t.slice(5).trim()                   // 去 "data:" 取 JSON
      JSON.parse(n) → choices[0].delta.content
  }
  ```
- **第一次尝试**（错误）：`createSSEParserTransform()` 剥离 `data:` 前缀，输出裸 JSON → 前端全部跳过
- **修正**：保留 `data: ` 前缀，只过滤 `: heartbeat` 注释行
- **Content-Type**：`text/event-stream`（前端据此判断流式模式）

### v3：非流式 chat 请求强制 `stream:false`
- **问题发现**：文本节点聊天用 `await T.json()` 解析响应（不支持流），网关 `chat/completions` 默认 `stream=true` 返回 SSE → 前端 `JSON.parse` SSE 失败
- **前端代码**（`httpClient-BknZwXjG.js` 位置 965457）：
  ```javascript
  let T = await Bl(v, {...body:JSON.stringify({model:..., messages:_, ...})})
  let E = (await T.json()).choices?.[0]?.message?.content || ''
  ```
- **修复**（`system.ts` `handleProxyJson`）：
  ```typescript
  if (body.url?.includes('/chat/completions') && !fetchBody.includes('"stream"')) {
      const p = JSON.parse(fetchBody);
      p.stream = false;   // 网关默认 true，前端用 T.json() 不支持
      fetchBody = JSON.stringify(p);
  }
  ```
- **验证**：curl 不带 `stream` → 返回纯 JSON `{"choices":[{...}]}` ✅

---

## P0-3：协议翻译层 + Content-Length 修复

### 信封剥离
- **文件**：`system.ts` `handleProxyFormData` 和 `handleProxyJson`
- **逻辑**：网关用 `ok(data)` → `{code:200, data}`。检测 `code` + `data` 字段 → 仅返回 `data`
- **安全降级**：非 JSON、无 `code` 字段、含 `error` → 原样透传

### Content-Length 不匹配（关键 bug！）
- **问题发现**：P0-3 改 body 后 Content-Length 仍是网关原值 → 浏览器等缺失字节 → `failed to fetch`
- **实测**：网关 `{code:200,data:[{task_id:...}]}` 98 字节，剥信封后 74 字节，差 24
- **修复**（4 处）：`skipHeaders` 加 `content-length`，`writeHead` 移到 P0-3 翻译之后
  ```typescript
  const skipHeaders = new Set(['transfer-encoding', 'connection', 'keep-alive', 
                                'content-encoding', 'content-length']);  // ← 新增 content-length
  // P0-3 翻译 ...
  res.writeHead(fetchRes.status, resHeaders);  // ← 移到翻译之后
  res.end(finalBody);
  // Node 自动用 Transfer-Encoding: chunked，不设固定 Content-Length
  ```
- **验证**：curl -i → `Transfer-Encoding: chunked`，无 `content-length` 头

---

## P0-4：异步生图/生视转同步（新增）

- **问题发现**：前端图片节点用 `t.data[0].url` 同步取图片 URL，但 flux.2-pro 是异步模型只返回 task_id → 显示"未生成"
- **前端代码**（`httpClient-BknZwXjG.js` 图片节点响应处理）：
  ```javascript
  // 非流式路径：
  let t = JSON.parse(e)
  if (R) {
      let e = t.data?.[0]               // 取 data[0]
      e?.b64_json ? ... : e?.url && (u = e.url)  // 取 url → 异步模型只返回 task_id
  }
  // u 为空 → 显示"未生成"
  ```
- **修复**（`system.ts` 新增 `handleAsyncPoll`）：
  - 检测 `/v1/images/generations|videos|video/generations|draw/completions` → 进入异步轮询
  - 提交任务 → 提取 `task_id` → 每 3s 轮询 `/v1/tasks/{task_id}` → 直到 `completed`
  - 提取 `result.images[0].url[0]` → 返回 `{code:200, data: [{url:"...", status:"completed"}]}`
  - 最多等 5min，超时/failed 返回错误
- **验证**：curl 图片提交 → 等待 ~20s → 返回 `{code:200, data:[{url:"https://a.lovart.ai/artifacts/..."}]}` → 前端显示图片 ✅

---

## 其他断点修复

### L1：kv/delete
- **文件**：`kv.ts`（新增函数）、`index.ts`（路由注册）
- **验证**：curl POST `/api/kv/delete?key=test` → `{"ok":true}` → GET 返回 `null`

### L3：版本号统一
- **文件**：`platform.ts:13`、`system.ts:11`、`index.ts:24`
- **改为**：`1.4.2`（原为 `1.3.5` / `2.0.0-maomao-clone`）
- **验证**：`/plugin/manifest.json` → `{"version":"1.4.2","hasUpdate":false}`

### S1：upload/thumbnail 绝对路径
- **文件**：`files.ts`
- **4 处返回点**加 `BASE_URL = http://127.0.0.1:18080` 前缀
- **验证**：chrome-extension:// 下图片不 404

### 高-2：画布宿主 + 自动开浏览器
- **文件**：`index.ts`
- **新增**：`handleFrontendPage()` 托管 dist/ 静态资源
- **新增**：`server.listen` 回调中 `execSync('open http://127.0.0.1:18080')`
- **路由兜底**：非 `/api/` `/plugin/` `/files/` 的 GET 请求 → 尝试 dist/ 文件

### A2：sync/default 本地兜底
- **文件**：`index.ts` 路由 + `data/apiConfigs.baseline.json`（新建）
- **逻辑**：读基线 JSON → 替换 `{VITE_API_BASE_URL}` → `http://127.0.0.1:18080`
- **基线 JSON 含**：lovart-chat(文本)、flux.2-pro/max、seedream-5(图片)、seedance-2.0-fast、kling-v3(视频)
- **验证**：`curl /api/sync/default` → `apiConfigs: 1 条`

### L5：assets/upload 别名
- **文件**：`index.ts` 路由
- **新增**：`/api/assets/upload`、`/api/upload/app-asset` → alias `handleUpload`

### G1/G2/G3：网关补齐
- **文件**：`main.py`
- G1：`GET /v1/videos/{id}` → `get_task(id, request)`
- G2：`GET /v1/video/generations/{id}` → `get_task(id, request)`
- G3：`POST /v1/draw/completions` → `_do_submit(client, body, "IMAGE")`

### I1：合并启动器
- **文件**：`launch-all.command`（新建，chmod +x）
- **流程**：① 启动网关 → ② 启动 localTool → ③ open 画布

### ESM 兼容
- **文件**：`index.ts`
- **问题**：`package.json` 声明 `"type":"module"` → `__dirname` 不可用 → 服务启动报错
- **修复**：`fileURLToPath(import.meta.url)` → 构造 `__filename` / `__dirname`

---

## 调试验证方法

### 启动服务
```bash
./launch-all.command                    # 一键启动
# 或分别启动：
cd apimart-gateway && ./启动网关.command &
cd localTool && node dist/index.js &
```

### 基础连通
```bash
curl http://127.0.0.1:18080/api/status     # localTool
curl http://127.0.0.1:9004/health           # 网关
curl http://127.0.0.1:18080/api/sync/default # apiConfigs
```

### 文本聊天
```bash
# 非流式（文本节点）：
curl -s -X POST http://127.0.0.1:18080/api/proxy \
  -H "Content-Type: application/json" \
  -d '{"url":"http://127.0.0.1:9004/v1/chat/completions","method":"POST",
       "body":"{\"messages\":[{\"role\":\"user\",\"content\":\"hi\"}],\"temperature\":0.7}"}'
# 返回: {"choices":[{"message":{"content":"Hi!"}},...]}

# 流式：
curl -s -N -X POST http://127.0.0.1:18080/api/proxy \
  -H "Content-Type: application/json" \
  -d '{"url":"http://127.0.0.1:9004/v1/chat/completions","method":"POST",
       "body":"{\"messages\":[{\"role\":\"user\",\"content\":\"hi\"}],\"stream\":true}"}'
# 返回: data: {"choices":[{"delta":{"content":"Hi!"}}]}\n\ndata: [DONE]

# 验证 SSE 解析：
grep 'proxy:stream' /tmp/localtool*.log  # 应有命中
# 验证无 heartbeat 残留：
grep 'heartbeat' /tmp/localtool*.log      # 应为空
```

### 图片生成
```bash
curl -s -X POST http://127.0.0.1:18080/api/proxy \
  -H "Content-Type: application/json" \
  -d '{"url":"http://127.0.0.1:9004/v1/images/generations","method":"POST",
       "body":"{\"prompt\":\"a red apple\",\"model\":\"flux.2-pro\",\"size\":\"1:1\"}"}'
# 等待 ~20s → {code:200, data:[{url:"https://...", status:"completed"}]}
```

### kv 操作
```bash
curl -X POST http://127.0.0.1:18080/api/kv/set -d '{"key":"t","value":"v"}'
curl "http://127.0.0.1:18080/api/kv/get?key=t"       # "v"
curl -X POST "http://127.0.0.1:18080/api/kv/delete?key=t"
curl "http://127.0.0.1:18080/api/kv/get?key=t"        # null
```

---

## 已确认的前端关键代码

### 流式 SSE 解析（`httpClient-BknZwXjG.js` 位置 951968）
```javascript
o.body.getReader() → TextDecoder → split('\n') → startsWith('data:') → JSON.parse
```

### 非流式 chat 解析（`httpClient-BknZwXjG.js` 位置 965457）
```javascript
await Bl(v, {...}) → await T.json() → choices[0].message.content
```

### 图片同步解析（`httpClient-BknZwXjG.js` 图片节点）
```javascript
t.data?.[0].url     // ← 必须保留 {data: [...]} 信封
```

### 代理请求格式（`Bl()` 函数 位置 537762）
```javascript
fetch(`${localToolBaseUrl}/api/proxy`, {
    body: JSON.stringify({url, method, headers, body, cookie})
})
// localToolBaseUrl = 127.0.0.1:18080
```

---

## 关键决策记录

| # | 决策 | 选项 | 选择 | 理由 |
|---|---|---|---|---|
| D1 | P0-3 剥信封范围 | 全剥 / 不剥 / 条件剥 | **不剥（data 为数组时保留）** | 前端图片用 `t.data[0].url`，数组必须保留信封；chat 响应 data 为对象时剥了也不影响 |
| D2 | 异步生图处理 | proxy 内部轮询 / 前端自己轮询 / 不处理 | **proxy 内部轮询后返回同步格式** | 前端图片节点只做同步解析(`t.data[0].url`)，不改前端就得 proxy 兜底 |
| D3 | SSE `data:` 前缀 | 剥离 / 保留 | **保留** | 前端 `startsWith('data:')` 硬编码，剥离会导致所有行被跳过 |
| D4 | 非流式 chat 的 stream 参数 | 透传网关默认(true) / 强制 false | **强制 false** | 文本节点 `await T.json()` 不支持流式，网关默认 true 会导致 JSON.parse 失败 |
| D5 | G1/G2 轮询上游 | Lovart tasks / kkidc / 先试 Lovart | **Lovart tasks** | 当前网关全走 Lovart，kkidc 需额外配置；先 alias 到 `/v1/tasks/{id}` 打通再说 |
| D6 | apiConfigs.baseline.json | 从官方抓取 / 手写最小配置 | **手写最小配置** | 官方地址不可达；包含核心文本+图片+视频模型即可 |
| D7 | Content-Length 处理 | 重算 / 删除让 Node 自动 | **删除** | P0-3 改 body 后长度变化不可预测；Node chunked 编码更可靠 |
| D8 | 图片异步轮询间隔 | 1s / 3s / 5s | **3s** | Lovart 生图一般 10-30s，3s 平衡响应速度与服务端负载 |
| D9 | 图片异步最大等待 | 1min / 5min / 10min | **5min（min(PROXY_TIMEOUT, 300000)）** | 与 proxy 超时对齐，大多数图片 <2min |

---

## 剩余未修复

| 断点 | 说明 | 状态 |
|---|---|---|
| A1 画布助手 | `/agent/{id}/chat` 走官方 API，未重定向到本地 | 待处理 |
| 中-4 ffmpeg | 需确认原版是否依赖 | 待定 |
| 中-5 预编译 | 提交 dist/ 或文档说明 | 待处理 |
| 低-4 安全 | 网关绑 0.0.0.0、CORS * | 待处理 |
| 低-2~9 | 缩略图、字段联调、听音、基址 | 待处理 |

## 关键文件速查

- **proxy 核心逻辑**：`localTool/src/routes/system.ts`
- **路由注册**：`localTool/src/index.ts`
- **网关端点**：`apimart-gateway/main.py`
- **apiConfigs**：`localTool/data/apiConfigs.baseline.json`
- **前端 proxy 调用**：`dist/assets/httpClient-BknZwXjG.js`（minified）
- **前端配置**：`dist/assets/endpointConfig-Bt85xi8d.js`（minified）
- **调试面板/探针**：`dist/background.js` + `dist/debug-panel.js`
